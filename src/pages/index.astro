---
import { defaultLang, languages } from "@/i18n";

const preferredLocale = Astro.preferredLocale;

if (preferredLocale && preferredLocale in languages) {
	return Astro.redirect(`/${preferredLocale}/`);
}

return Astro.redirect(`/${defaultLang}/`);
---
